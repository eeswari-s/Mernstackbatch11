Promise:
           it holds the result of async operatiomn.
           Need of promise: to overcome callbackhell scenario.
           it is an object, which is holds result .


    syntax:
            var variablename = new promise((reolve, reject));

    promise have 3 states:
     1.pending
     2.fulfilled(resolveall)
     3.rejected
=====================================================================================
2.promiseall
====================================================================================
3.promise chaining
=================================================================================
4.then.catch
=================================================================================
5prototype:
